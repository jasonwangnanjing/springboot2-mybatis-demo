<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- mapper:根标签，namespace：命名空间，随便写，一般保证命名空间唯一 -->
<mapper namespace="com.jw.dao.MaterialDao">
    <!-- statement，内容：sql语句。id：唯一标识，随便写，在同一个命名空间下保持唯一
       resultType：sql语句查询结果集的封装类型,tb_user即为数据库中的表
     -->
    <select id="selectMaterial" resultType="Material">
      select * from tb_item where id = #{id}
   </select>
    <update id="updateMaterial" parameterType="Material">
       update tb_item set
                      tb_item.itemName = #{Material.itemName}
                      tb_item.itemPrice = #{Material.itemPrice}
                      tb_item.itemDetail = #{Material.itemDetail}
                      where tb_item.id = #{Material.id}
   </update>

    <delete id="deleteMaterial" parameterType="int">
        delete from tb_item where id = #{id}
    </delete>
    <insert id="createMaterial" parameterType="Material">

        insert into tb_item ( itemName, itemPrice,itemDetail ) values  ( #{Material.itemName}, #{Material.itemPrice},#{Material.itemDetail} )

    </insert>
</mapper>
